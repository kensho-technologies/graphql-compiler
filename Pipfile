[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[packages]  # Make sure to keep in sync with setup.py requirements.
pytz = ">=2017.2"
six = ">=1.10.0"
sqlalchemy = ">=1.3.0,<2"

[requires]
python_version = "3.8"
