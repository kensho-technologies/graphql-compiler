language: python
cache: pip
dist: bionic
services:
  - docker
install:
  - pip install --upgrade pip
  - pip install --upgrade pipenv
matrix:
  include:
  - name: "Lint and static analysis"
    python: "3.8"
    script:
      pipenv --support
